bquant_duckdb:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: /storage/hhbach/bquant/warehouse/bquant.duckdb
      threads: 4
      schema: analytics
      config_options:
        threads: 4
        memory_limit: 4GB
